<IfModule mod_rewrite.c> 
  RewriteEngine On 
  RewriteBase / 

  # Allow direct access to existing files & folders 
  RewriteRule ^index\.html$ - [L] 
  RewriteCond %{REQUEST_FILENAME} -f [OR] 
  RewriteCond %{REQUEST_FILENAME} -d 
  RewriteRule ^ - [L] 

  # Fallback all routes to React index.html 
  RewriteRule ^ index.html [L] 
</IfModule>
